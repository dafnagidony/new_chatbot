var module = require('..');
var test = require('tape');


test('Module', {skip: false}, function(t) {
  t.equal(typeof module, 'object', 'returns object');
  t.test('should export versions', function(tt) {
    tt.equal(typeof module.versions, 'object', 'as object');
    tt.end();
  });
  t.end();
});
