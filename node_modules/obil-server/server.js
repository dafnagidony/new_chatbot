var server = require('.');
var pkg = require('./package');

server.start({
  port: 6543,
  app: {
    session: {
      secret: 'foo'
    },
    aclFree: 1,
    locals: {
      name: 'foo',
      version: pkg.version,
      dependencies: pkg.dependencies
    }
  }
});
